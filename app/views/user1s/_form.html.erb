<%= form_with(model: user1, local: true) do |form| %>
  <% if user1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user1.errors.count, "error") %> prohibited this user1 from being saved:</h2>

      <ul>
      <% user1.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :user1_name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :user1_email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
